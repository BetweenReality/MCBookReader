# Minecraft Book Reader

Small program to read exported books outside of the game. Renders as the in-game book UI as faithfully as possible.

Currently only supports nbt-formatted books.

## TODO
- Obfuscated text (currently displays regular text with a highlight instead)
- Improved font rendering (some styles aren't accurate to in-game)
- DPI adjustment (currently hardcoded to 2x)
- Title page with options
- Plaintext format parsing
- (Maybe) Book editor / saving
- Test on Linux (there's Windows-specific code)